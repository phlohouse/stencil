name: matrix
description: "A transposed table: one record per column"
discriminator:
  cells:
    - A1
versions:
  v1.0:
    fields:
      matrix_table:
        range: A3:D
        type: table
        orientation: vertical
        columns:
          "3": record_name
          "4": hb
          "5": wbc
